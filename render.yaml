services:
  - type: web
    name: koikoi-api
    runtime: python
    pythonVersion: 3.11
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
